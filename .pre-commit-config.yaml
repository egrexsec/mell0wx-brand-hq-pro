repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.25.1
    hooks:
      - id: gitleaks
        name: gitleaks (staged)
        args:
          - "--config=.gitleaks.toml"
          - "--staged"
          - "--redact"
